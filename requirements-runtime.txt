# Pinned CPU runtime used by the Docker inference image.
# Matches the environment recorded in the V10, V13, and V14 artifacts.
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.18.0
pyarrow==25.0.1
xgboost==3.4.0

# HTTP serving dependencies. The model runtime above remains artifact-pinned.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
